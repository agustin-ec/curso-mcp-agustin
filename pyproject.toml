[project]
name = "curso-mcp-agustin"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai>=2.21.0",
    "python-dotenv>=1.2.3",
    "rich>=15.0.0",
]

[project.scripts]
curso-mcp-agustin = "curso_mcp_agustin:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"

[tool.uv.workspace]
members = [
    "vibe-coding",
]
