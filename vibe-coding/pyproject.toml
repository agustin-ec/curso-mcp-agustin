[project]
name = "vibe-coding"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ernesto Agustín Quintana Valenzuela", email = "ernestoquintanav@hotmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
vibe-coding = "vibe_coding:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
